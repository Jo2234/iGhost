# Generate with: openssl rand -hex 32
IGHOST_ACCESS_TOKEN=
OPENAI_API_KEY=
PORT=4173
# Local default. Docker sets 0.0.0.0 behind HTTPS.
IGHOST_BIND_HOST=127.0.0.1
# Optional GitHub delivery: one allowed repository and scoped token.
IGHOST_REPO_URL=
IGHOST_GITHUB_TOKEN=
